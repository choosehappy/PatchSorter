version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.14"
  jobs:
    pre_build:
      - pip install --no-deps -e .
      - pip install -r docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py