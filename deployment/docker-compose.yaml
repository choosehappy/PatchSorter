services:
  db:
    build:
      context: .
      dockerfile: Dockerfile.citus-postgis
    container_name: citus_single
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"

  