services:
  qa:
    user: ray
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    image: histotools/patchsorter:local
    command: 
      /bin/bash
    volumes:
      - ..:/opt/PatchSorter
    cap_add:
      - SYS_PTRACE